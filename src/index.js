import { router, dom } from '@artevelde-uas/canvas-lms-app';


export default function () {
    router.onRoute('courses.gradebook.speedgrader', () => {

    });

    return {
        ...require('../package.json')
    };
}
